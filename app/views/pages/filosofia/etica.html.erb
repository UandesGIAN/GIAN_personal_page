<!-------------------------------->
<!--  3. LOS ELEMENTOS DE VIDA  -->
<!-------------------------------->

<div class="relative flex items-center mt-20 mb-10 justify-center h-full animated-element-slide-r">
  <h1 class="font-bold text-6xl md:text-8xl sm:text-8xl xs:text-4xl text-center mb-2 text-shadow-orange filosofia-home animate-slide-in-right">
    Filosofía de GIAN<p class="text-gray-500 mt-10" style="text-shadow: 0px 2px 20px rgba(107, 114, 128, 0.6);">ÉTICA</p>
  </h1>
</div>

<%= @indice_content %>

<!-- ESTRUCTURA -->
<div class="text-container max-w-5xl mx-auto mt-20 px-4 flex flex-col pb-8">
  <section id="bg-articulos" class="p-6 
    <%= 'bg-gray-200 sm:bg-gray-300 text-gray-800' if @modo_luz == 'claro' %> 
    <%= 'bg-gray-900 text-gray-300' if @modo_luz == 'oscuro' %> 
    rounded-2xl shadow-md flex-grow">
    <!-- DESCRIPCION Y CONTENIDO -->
    <p class="mb-6 text-lg ">
        <i>Work In Progress...</i>
    </p>

    <!-- SECCIONES -->
    <h2 class="text-5xl mt-14 text-center font-bold mb-4 text-gray-500">Secciones:</h2>
    
    <a href="<%= filosofia_subsection_path(section: 'etica') %>" class="text-gray-500 text-center">
    </a>
    <% if @subsections['Ética'].present? %>
      <div class="flex flex-col items-center"> 
        <ul class="list-inside ml-12 mt-2">
          <% @subsections['Ética'].each_with_index do |sub_section, index| %>
            <li>
              <a href="<%= filosofia_subsection_path(section: 'etica', sub_section: sub_section.downcase.gsub(/[áéíóúñ]/, 'á' => 'a', 'é' => 'e', 'í' => 'i', 'ó' => 'o', 'ú' => 'u', 'ñ' => 'n').gsub(' ', '_')) %>" class="text-gray-500 text-2xl font-bold underline">
                <%= "#{index + 1} #{sub_section}" %>
              </a>
            </li>
          <% end %>
        </ul>
      </div>
    <% end %>

</div>