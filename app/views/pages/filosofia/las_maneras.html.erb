<!-------------------------------->
<!--       2. LAS MANERAS       -->
<!-------------------------------->

<div class="relative flex items-center mt-20 mb-10 justify-center h-full animated-element-slide-r">
  <h1 class="font-bold text-6xl md:text-8xl sm:text-8xl xs:text-4xl text-center mb-2 text-shadow-orange filosofia-home animate-slide-in-right">
    Filosofía de GIAN<p class="text-yellow-500 mt-10" style="text-shadow: 0px 2px 20px rgba(200, 150, 10, 0.6);">LAS MANERAS</p>
  </h1>
</div>

<%= @indice_content %>

<!-- ESTRUCTURA -->
<div class="text-container max-w-5xl mx-auto mt-20 px-4 flex flex-col pb-8">
  <section id="bg-articulos" class="p-6 
    <%= 'bg-gray-200 sm:bg-gray-300 text-gray-800' if @modo_luz == 'claro' %> 
    <%= 'bg-gray-900 text-gray-300' if @modo_luz == 'oscuro' %> 
    rounded-2xl shadow-md flex-grow">
    <!-- DESCRIPCION Y CONTENIDO -->
    <p class="mb-6 text-lg ">
        A continuación se mencionan <strong>LAS MANERAS</strong> que Gian ha determinado para <strong>intentar</strong> ayudar a alguien a entender o aplicar <a href="<%= filosofia_subsection_path(section: 'el_metodo_de_gian') %>" class="text-lg underline">
            EL MÉTODO DE GIAN</a>.
        Cabe decir que este apartado final va dirigido <strong>a quien comparta el método</strong>, 
        <strong>no a quien lo reciba</strong>; y sólo debe utilizarse en dos casos: <br>Al tratar de ayudar alguien que <strong>ACEPTA</strong> tu ayuda y la solicita<br>O al ser interceptado por un ente que supone un obstáculo de vida (ósea que se interpone en tu camino y requieres que deje de hacerlo)...<br><br>
        Pues son las formas en las que puedes hacerle <em>pensar, ver, sentir, ser</em> y <em>hacer</em> cosas al otro si consideras que es lo mejor para ese individuo, ya sea dándole las herramientas, explicándoles cómo
         se usan o haciendo el trabajo difícil por estas personas, pero esto, dependerá de tí, <strong>quien está buscando ayudar</strong> (solo 
        te pido que recuerdes <a href="<%= filosofia_subsection_path(section: 'el_metodo_de_gian') %>" class="text-lg underline">
            EL MÉTODO DE GIAN</a> y aquello que eres, para <em>mejorar</em> tu postura y así ayudar de forma más <em>eficiente, justa</em> y <em>positiva</em> a quien lo necesite).
    </p>

    <!-- SECCIONES -->
    <h2 class="text-5xl mt-14 text-center font-bold mb-4 text-yellow-500">Secciones:</h2>
    
    <a href="<%= filosofia_subsection_path(section: 'las_maneras') %>" class="text-yellow-500 text-center">
    </a>
    <% if @subsections['LAS MANERAS'].present? %>
      <div class="flex flex-col items-center"> 
        <ul class="list-inside ml-12 mt-2">
          <% @subsections['LAS MANERAS'].each_with_index do |sub_section, index| %>
            <li>
              <a href="<%= filosofia_subsection_path(section: 'las_maneras', sub_section: sub_section.downcase.gsub(/[áéíóúñ]/, 'á' => 'a', 'é' => 'e', 'í' => 'i', 'ó' => 'o', 'ú' => 'u', 'ñ' => 'n').gsub(' ', '_')) %>" class="text-yellow-500 text-2xl font-bold underline">
                <%= "#{index + 1} #{sub_section.capitalize}" %>
              </a>
            </li>
          <% end %>
        </ul>
      </div>
    <% end %>

</div>